{
  "version": 2,
  "builds": [
    {
      "src": "gulpfile.js",
      "use": "@vercel/node",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
